import 'package:flutter/material.dart';

class AppPalette {
  static const Color primaryGreen = Color.fromARGB(255, 156, 237, 158);   // Growth & trust
  static const Color soilBrown = Color(0xFF6D4C41);      // Earthy support
  static const Color sunnyYellow = Color(0xFFFDD835);    // Energy & harvest
  static const Color neutralLight = Color(0xFFFAF9F6);   // Clean background
  static const Color neutralDark = Color(0xFF424242);    // Text & contrast

  // Optional extras
  static const Color deepForest = Color(0xFF2E7D32);     // Alternative green
  static const Color terracotta = Color(0xFFE2725B);     // Soil vibe
  static const Color skyBlue = Color(0xFF64B5F6);        // Trust & peace
  static const Color transparent = Colors.transparent;        // Trust & peace
}
